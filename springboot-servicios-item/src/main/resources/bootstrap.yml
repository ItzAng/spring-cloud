server:
  port: 8002
spring:
  cloud:
    config:
      uri:
      - http://localhost:8889
  profiles:
    active:
    - dev  
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'